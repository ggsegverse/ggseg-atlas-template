on:
  push:
    branches: [main, master]
    paths:
      - DESCRIPTION
      - .github/workflows/update-codemeta.yaml
  workflow_dispatch:

name: update-codemeta

permissions: read-all

jobs:
  update-codemeta:
    # The repository check skips the template itself, where the placeholder
    # sources are not a valid R package. Runs normally in generated packages.
    if: >-
      github.repository != 'ggsegverse/ggseg-atlas-template' &&
      !contains(github.event.head_commit.message, 'cm-skip')
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::codemetar, local::.

      - name: Update codemeta.json
        run: codemetar::write_codemeta()
        shell: Rscript {0}

      - name: Commit and push
        if: github.event_name == 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add codemeta.json
          git diff --staged --quiet || (git commit -m "Update codemeta.json" && git push)
